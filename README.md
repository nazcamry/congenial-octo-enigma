# 🏆 Alpha Bank Income Predictor (Hackathon Solution)

**Решение кейса по предсказанию доходов клиентов и формированию персональных предложений.**

![Status](https://img.shields.io/badge/Status-Completed-success)
![Python](https://img.shields.io/badge/Python-3.11-blue)
![Docker](https://img.shields.io/badge/Docker-Ready-2496ED)

## 📌 Описание решения
Система представляет собой **Full-stack AI сервис**, который:
1.  Принимает профиль клиента (или выбирает пресет).
2.  Использует ансамбль ML-моделей (Stacking) для точного прогноза дохода.
3.  Объясняет прогноз с помощью **SHAP values** (интерпретируемость).
4.  Формирует персональные продуктовые рекомендации.

### 🧠 ML Ядро (Stacking)
Для достижения минимального **WMAE** используется трехуровневая архитектура:
*   **Level 1:** CatBoost, LightGBM, XGBoost (обученные на 150+ признаках).
*   **Level 2 (Meta):** Ridge Regression для взвешивания прогнозов базовых моделей.
*   **Особенности:** Обработка категориальных признаков, логарифмирование таргета, WMAE-оптимизация через Optuna.

---

## 📂 Структура проекта

```text
submission/
├── backend/                 # Серверная часть (FastAPI)
│   ├── models/              # Обученные модели (.pkl, .cbm)
│   ├── app.py               # Основной файл API
│   ├── Dockerfile           # Инструкция для сборки бэкенда
│   └── requirements.txt     # Зависимости Python
│
├── frontend/                # Клиентская часть (Streamlit)
│   ├── app.py               # Интерфейс дашборда
│   └── Dockerfile           # Инструкция для сборки фронтенда
│
├── docker-compose.yml       # Оркестрация контейнеров
└── README.md                # Документация
```

## 🚀 Как запустить проект

### Вариант 1: Быстрый запуск через Docker (Рекомендуется)
Если у вас установлен Docker Desktop:

1. **Склонируйте репозиторий:**
   ```bash
   git clone https://github.com/nazcamry/congenial-octo-enigma.git
   cd congenial-octo-enigma

2. **Запустите сборку и старт:**
    ```bash
    docker-compose up --build





Ссылка на видео в GoogleDrive:
https://drive.google.com/file/d/1b_HvksFInuErb1B99OTGwx_U59PCrQ6s/view?usp=sharing

3. **Откройте в браузере:**
    Frontend (Дашборд): http://localhost:8501
    Backend (Docs): http://localhost:8000/docs
